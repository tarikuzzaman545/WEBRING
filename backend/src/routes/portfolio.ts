import { Router } from "express"; import { getPortfolio, addPortfolio, updatePortfolio, deletePortfolio, reorderPortfolio } from "../controllers/portfolioController"; import { requireAuth } from "../middleware/auth"; const router = Router(); router.get("/", getPortfolio); router.post("/", requireAuth, addPortfolio); router.put("/reorder", requireAuth, reorderPortfolio); router.put("/:id", requireAuth, updatePortfolio); router.delete("/:id", requireAuth, deletePortfolio); export default router;
