import { Router } from "express"; import { addBooking, getBookings, updateBookingStatus, getDates, updateDates } from "../controllers/bookingController"; import { requireAuth } from "../middleware/auth"; const router = Router(); router.post("/", addBooking); router.get("/", requireAuth, getBookings); router.put("/:id/status", requireAuth, updateBookingStatus); router.get("/dates", getDates); router.put("/dates", requireAuth, updateDates); export default router;
