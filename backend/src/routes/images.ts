import { Router } from "express"; import { getImages, uploadImage, deleteImage } from "../controllers/imageController"; import { requireAuth } from "../middleware/auth"; import { upload } from "../middleware/upload"; const router = Router(); router.get("/", getImages); router.post("/upload", requireAuth, upload.single("image"), uploadImage); router.delete("/:category/:key", requireAuth, deleteImage); export default router;
